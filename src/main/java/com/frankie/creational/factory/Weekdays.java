package com.frankie.creational.factory;

// make user select from enum rather than passing string to RoutinePlanner.getRoutine() method
public enum Weekdays {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY
}
